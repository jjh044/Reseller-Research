{
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check src/app.js && node --check server.js && node --check api/config.js && node scripts/mobile-release-check.js",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy"
  },
  "dependencies": {
    "@clerk/backend": "^2.19.0",
    "convex": "^1.25.4"
  },
  "engines": {
    "node": "22.x"
  }
}
